
for (let i = 1; i <= 12; i++) {
    console.log(i);
}

for (let u = 0; u <= 20; u += 2 ) {
    console.log(u)
}

for (let down = 100; down >= 0; down -= 10)
{console.log(down)
}

const family = ['Mother', 'Grandmother', 'Grandfather', 'Sister', 'Father', 'Uncle', 'Auntie', 'first_cousin']

for (let measure = 0; measure < family.length; measure += 1)
{ console.log(measure, family[measure])
}

